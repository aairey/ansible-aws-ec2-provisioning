---
- name: "Create EC2 instance"
  hosts: localhost
  user: centos
  tags:
    - provision
  vars_files: 
    - vars.yml
  pre_tasks:
    - debug: msg="print {{instances}}"
  roles:
    - { role: aws-ec2-provisioning }
